#include "stdafx.h"
#include <iostream>
#include <math.h>

using namespace std;

int main() {
	int N, aux, n, a;
	cin>>N;
	a=N;
	while(a>0){
		aux=a%10;
		if (aux%2==0) n=n+1;
		a=a/10;
	}
cout<<n;
	return 0;
}
